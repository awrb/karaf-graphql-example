type Query {
  bookById(id: ID): Book
  books: [Book]
}

type Book {
  id: ID
  name: String
  pageCount: Int
  authorId: String
}

type Mutation {
    addBook(name: String, pageCount: Int, authorId: String): Book
}