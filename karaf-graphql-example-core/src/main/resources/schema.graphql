schema {
  query: Query
  mutation: Mutation
}

type Query {
  bookById(id: ID): Book
  books: [Book]
}

type Book {
  id: ID
  name: String
  pageCount: Int
}

type Mutation {
    addBook(name: String, pageCount: Int): Book
}